# EventStoreML
EventStoreML is a minimal, self-hosting markup language where schemas are declared as events. Its core consists of exactly one must-understand event type – core.TypeDeclared@1 – expressed in pure JSON Schema. Everything else is built, declared, and evolved on top of it.
